import * as React from "react";
import {Header} from './../../components';
const Home = (props) => {

    const render = () => {
        return (
            <div>
                <Header></Header>
            </div>
        )
    }

    return render()
}

export default Home;